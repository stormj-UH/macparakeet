# MacParakeet v0.7.3 Comprehensive Release and Architecture Audit

> **Canonical session handoff**
>
> Date: 2026-07-16
>
> Audited baseline: origin/main at **6599603c0f775d32009422a7ddad6385a8a932df**
>
> Stable release fixed point: **v0.7.2**, commit **afc2eff9dd0c388f6833f801dad139a8b09bfc0a**
>
> Candidate: **MacParakeet 0.7.3**
>
> Audit branch: **codex/release-readiness-20260716**

This document is the durable, consolidated record of the July 16 review. It
combines the recent-change review, whole-codebase and architecture deep dive,
release-candidate evidence, live issue and pull-request interpretation, fixes
made while auditing, residual risks, and the exact path to publication.

The subsequent automated and CLI-focused execution record is in
[`2026-07-16-v0.7.3-qa.md`](./2026-07-16-v0.7.3-qa.md).

The app should remain on the **0.7.x** train. The bundled command-line tool has
its own 3.0 contract version; that does not imply an app 0.8 release.

## 1. Executive verdict

### Code verdict

The audited main commit is a credible **0.7.3 code candidate** after the
dependency, build-tooling, and canonical-documentation repairs on this audit
branch. No reviewed defect in persistence, privacy, capture ownership, meeting
settlement, engine routing, or the public CLI contract requires a 0.8 release
or a broad architectural rewrite.

### Shipping verdict

The candidate is **code ready, not ship complete**. It still needs a
Developer-ID-signed, notarized, installed-app candidate and the physical audio
matrix in section 15. The audit deliberately did not tag, sign, notarize,
upload, alter the appcast, publish a GitHub release, or update Homebrew.

### Architecture verdict

The architecture is healthy and has several legitimately deep modules. The
best next improvements are concentrated, not structural churn:

1. Separate speech-model lifecycle policy from STTRuntime.
2. Make explicit microphone selection authoritative instead of silently
   falling through to another input.
3. Create one read-only Speech Memory query and citation meaning for Library,
   CLI, and future agent adapters.
4. Add a current-toolchain Swift concurrency gate and fix the newly exposed
   Sendable seam.
5. Deepen transcript-detail and Settings presentation by behavior domain,
   without mechanically splitting large files.

## 2. Audit method and scope

The review was performed in a clean worktree created from freshly fetched
origin/main. The user's dirty archive checkout was not changed.

The review covered:

- Every first-parent merge after v0.7.2 through PR #822.
- The complete v0.7.2-to-main diff: **143 files, 9,972 insertions, and 1,416
  deletions**.
- **462** source Swift files and **289** test Swift files.
- **28** architecture decision records, **4** boundary contracts, subsystem
  READMEs, the spec index, active/completed plans, distribution scripts, package
  constraints, and release metadata.
- Capture ownership and teardown, microphone routing, meeting recovery and
  finalization, STT routing and leases, model lifecycle, database repositories
  and migrations, auto-save, filenames, CLI compatibility, telemetry,
  diagnostic logging, privacy, entitlements, update trust, and packaging.
- Live GitHub release, appcast, release-download size, exact-main CI, open
  issues, repository security settings, and PR #822 through its reviewed merge.
- Static searches for forced casts and operations, deliberate crash paths,
  credential-like data, tracked diagnostics, and diff hygiene.

Generated directories, private diagnostics folders, journal material, and
unrelated worktrees were excluded unless they were directly relevant. The nine
tracked diagnostic logs were reviewed specifically for accidental sensitive
content.

## 3. Live release truth

### Current public release

- Latest GitHub release: **v0.7.2**
- Release name: **MacParakeet 0.7.2**
- Published: **2026-07-12 20:27:06 UTC**
- Draft/prerelease: **no / no**
- Dereferenced release commit: **afc2eff9dd0c388f6833f801dad139a8b09bfc0a**

The canonical Sparkle feed is **https://macparakeet.com/appcast.xml**. Its
newest item is 0.7.2, build 20260712195419, minimum macOS 14.2. The enclosure
length is 148,685,155 bytes and the remote DMG reports the same content length.
The feed contains an EdDSA signature.

### Audited development head

- origin/main: **6599603c0f775d32009422a7ddad6385a8a932df**
- Exact-main CI run: **29532380793**
- CI result: **success**
- Created/completed: **2026-07-16 20:31:00–20:47:00 UTC**

### Candidate bundle produced during the audit

The bundle was produced at the pre-#822 baseline, then #822 landed as reviewed
Settings presentation/spec work. It is evidence for the strict build path, not
the final distributable; the signed candidate must be rebuilt from 6599603c
plus the audit branch's release fixes and canonical documentation commits.

The strict unsigned build generated **dist/MacParakeet.app** with:

| Property | Verified value |
|---|---|
| App version | 0.7.3 |
| Build | 20260716195415 |
| Minimum macOS | 14.2 |
| Feed | https://macparakeet.com/appcast.xml |
| Embedded Git SHA | ef9d2509c32e |
| Build source | dist-xcodebuild-release |
| Architecture | arm64 |
| Bundled CLI contract | 3.0.0 |
| Sparkle public key | present |
| Signature state | ad-hoc/linker only; not Developer ID |
| App executable UUID | 4D1A61A3-FC70-3D25-A589-2DE18AE44672 |
| dSYM UUID | 4D1A61A3-FC70-3D25-A589-2DE18AE44672 |

The unsigned state is expected for this audit artifact. It must not be
distributed as the release candidate.

## 4. Complete post-v0.7.2 change ledger

The first-parent sequence reviewed was:

| Change | Review interpretation |
|---|---|
| 6affbe7f, f27d6eab | Repository screenshot uploads only. |
| PR #801, 01e3285a | Restores implicit System Default microphone semantics; directly relevant to Bluetooth routing reports. |
| PR #783, 8369d62e | Separates live and final STT workflows and makes routing roles explicit. |
| PR #775, fcaca843 | Adds CLI installation and integration workflow. |
| PR #795, ec961b37 | Corrects Anthropic temperature behavior. |
| PR #785, d1f61181 | Adds dictation cold-start/prewarm reliability work. |
| 3b6ae216, 89cb0d13 | Diagnostic-log additions; reviewed for sensitive content. |
| PR #802, 973435e6 | Release-readiness and CLI v3 contract work. |
| PR #810, 9bf4ac5c | Fixes meeting-stop stack overflow. |
| PR #811, d9772036 | Corrects Nemotron spacing behavior. |
| PR #812, 0667d1cd | Surfaces preparation progress. |
| PR #813, c6f94540 | Defines live/final engine roles. |
| PR #814, e5aadba6 | Hardens bounded capture lifecycle and teardown. |
| PR #816, 4802b477 | Bounds yt-dlp execution with a timeout. |
| PR #815, 8fa6b496 | Persists edited filenames safely. |
| PR #817, 6eb21fa7 | Handles multilingual filler-word behavior. |
| b33c013b | Additional diagnostic evidence; reviewed for sensitive content. |
| PR #818, 9fec5043 | Makes auto-save destinations and failure feedback reliable. |
| PR #819, ef9d2509 | Adds the current Engine settings surface. |
| PR #822, 6599603c | Clarifies the Advanced transcription-engine option, keeps errors visible, and makes Settings search expand/scroll to the control. |

No new correctness, privacy, persistence, or public-contract defect was found
in those merged changes. The highest-risk flows have explicit ownership:

- ScreenCaptureKit startup exposes partial ownership to Stop immediately.
- A late startup completion cannot revive or erase a stopped meeting.
- Audio callbacks target immutable capture state and drain before release.
- Live and final STT routes are snapshotted, leased, and attributed explicitly.
- Final-model loading remains lazy.
- Auto-save bookmark mutation retains race guards and surfaces failure.
- Filename persistence uses parameterized GRDB updates.

## 5. Subsystem review

### Capture and microphone routing

The shared microphone and meeting code now has substantially better ownership,
route-attempt diagnostics, prewarming, and teardown tests. PR #801 correctly
restores System Default as an implicit macOS route rather than coercing it to
the built-in microphone.

One policy remains intentionally called out for later work:
meetingInputDeviceAttempts tries a selected input, then System Default, then the
built-in microphone. That makes capture more likely to start, but a named input
can silently become a different input. This is a user-intent and privacy
contract, not merely an implementation detail.

### Meeting lifecycle, recovery, and AEC

The reviewed stop/start/finalize paths have bounded state transitions, recovery
artifacts, and explicit settlement. PRs #810 and #814 address credible failure
modes in the old stop path. AEC and cleaned-microphone artifacts are included
in strict packaging and checksum gates.

No code-level blocker was found. Real-call validation remains mandatory because
echo cancellation quality and hardware routing cannot be proven by unit tests.

### STT runtime and engine scheduling

STTRuntime and STTScheduler are the correct central control plane for engine
ownership, warm-up, leases, and inference ordering. PRs #783 and #813 improve
role clarity rather than duplicating runtimes.

The main architectural pressure is model lifecycle: download, availability,
cache layout, deletion, and telemetry are spread across static runtime/engine
methods and several GUI/CLI callers. FluidAudio 0.15.5 removing DownloadUtils
made that wide coupling visible.

### Persistence and user data

GRDB repositories remain useful seams, roughly one per table. DatabaseManager
is large because it is the chronological migration ledger; splitting it would
make data evolution harder to audit. No unsafe destructive recovery behavior,
unparameterized filename update, or migration-order defect was found.

Meeting artifacts and the database continue to be treated as user data.

### Text processing

The shared pipeline correctly centralizes ordered dictation/transcription work.
There is no evidence for creating another pipeline or splitting it by caller.
The recent multilingual and spacing changes are bounded by focused behavior
tests.

### CLI and agent-facing contract

The bundled CLI reports contract version 3.0.0 and its help/version paths work.
The public CLI has an independent semantic contract from the GUI app version,
so MacParakeet 0.7.3 plus CLI 3.0.0 is coherent.

The unsigned standalone CLI emits a nonfatal NSUserDefaults suite warning. The
installed, signed application path remains the authoritative release smoke.

### Update, distribution, and trust

The appcast feed, embedded public key, minimum OS, bundled helpers, legal
notices, model/runtime checksum, and matching dSYM were checked. The build
script now diagnoses Xcode's missing git-submodule helper and borrows the shell
Git helper directory when safe.

### Privacy, telemetry, and diagnostics

The local-first boundaries remain intact. Cloud transforms, downloads, update
traffic, and telemetry are explicit surfaces. A targeted scan of the nine
tracked diagnostic logs found no credential, token, prompt, transcript, email,
user-path, or URL disclosure matching the audit patterns.

Raw logs still should not be committed as routine debugging practice. Prefer
private attachments or explicitly redacted/synthetic fixtures.

### Documentation and plans

The canonical docs had lagged the product: they still called 0.6.24 stable and
described several shipped v0.7 capabilities as pending. The audit branch
corrects stable/candidate framing, shipped auto-stop/VAD/live-preview behavior,
and recent plan status. The capability-registry plan itself is complete for
Phase A; later phases remain separate gated work.

## 6. Live issue matrix

All issues below were still open when verified.

| Issue | Evidence and current interpretation | 0.7.3 action |
|---|---|---|
| #796 | v0.7.2 Input Test failed; reporter said 0.6.24 worked. This is consistent with the forced-input routing regression addressed by #801. | Must verify with physical Bluetooth/System Default routes. |
| #820 | v0.7.2 no-audio report; owner explicitly identified input routing and said a fix was ready. #801 is the intended repair. | Must verify on affected hardware class. |
| #803 | v0.7.1 WhatsApp dictation only worked when speaking very loudly. Bluetooth was not confirmed. | Likely routing-related, but evidence is incomplete; include in device matrix. |
| #798 | Second/repeated Instant Dictation recording fails; no diagnostics were attached. #785 is a plausible repair, not proof of closure. | Repeat/idle/toggle/sleep-wake test. |
| #808 | Two-hour/stuck meeting state persisted even after quit. #810/#814 address credible causes, but no reporter reproduction has confirmed it. | Long meeting and stop/finalize test. |
| #605 | Speaker bleed in no-headphones meetings. AEC shipped in v0.7 and packaging gates pass. | Real Zoom/Meet/Teams validation required. |
| #562 | Reporter showed Parakeet v3 dropping/empty output while v2 and Unified worked, and also reported short-utterance “more audio please” behavior across models. | Keep both concerns visible; support workaround and do not overclaim closure. |
| #470 | Focusrite multichannel input/channel selection mismatch. Current code downmixes all channels, tries the selected input first, and has regression tests. | Likely code-addressed, but close only after physical USB validation. |

The issue state is not proof that the candidate is broken; it is a record of
where physical evidence is still needed. Do not auto-close these issues solely
from code inspection.

## 7. PR #822 final state

### PR #822 — Clarify advanced transcription engine option

At review time:

- Head SHA: **d2b98d5f391e863e76770dfd8118fa34d894aacf**
- Base SHA: **ef9d2509c32eba8859bd3eab5dabf19474254293**
- Diff: **6 files, +138 / -83**
- Review threads: all resolved at that head
- Final state: merged at 2026-07-16 20:30:57 UTC
- Merge commit: **6599603c0f775d32009422a7ddad6385a8a932df**
- Exact-head checks: both CI runs, CodeRabbit, and Greptile green; Cubic was
  neutral rather than failing

The original UX problem was real: collapsing the Advanced disclosure also hid
transcription-engine errors. The head revision moves errors outside the
disclosure, shares the search anchor, expands and scrolls navigation targets,
and uses the accurate term “eligible” rather than “downloaded.”

This is presentation/search/spec polish. It does not change capture routing,
engine persistence, or release packaging. Its exact reviewed head was green,
it is now included in the audited main SHA, and the audit branch was rebased
onto its merge commit.

PRs #537 and #363 are older conflicting/dirty work and should not be pulled
into the release.

## 8. Release findings fixed on the audit branch

### 8.1 FluidAudio dependency range

Package.swift allowed FluidAudio 0.15.4 up to, but excluding, 0.16.0.
Package.resolved happened to contain 0.15.4 at revision
b9d43724cbdb5a980e441fd54180964e94d470f7. Version 0.15.5 replaces DownloadUtils
with ModelHub, while MacParakeet still invokes DownloadUtils in Nemotron and
other lifecycle paths.

The range could therefore resolve to a source-incompatible patch on a fresh
release build. The audit branch exact-pins 0.15.4. This is a release
stabilization measure, not a permanent refusal to upgrade. ModelHub migration
belongs in the speech-model lifecycle work.

### 8.2 Xcode missing git-submodule

The release machine reports Xcode 26.4.1 and Swift 6.3.1. With GIT_EXEC_PATH
unset, xcrun Git cannot run git submodule because Xcode's Apple Git installation
does not contain the git-submodule helper. Homebrew Git does contain it at:

**/opt/homebrew/opt/git/libexec/git-core/git-submodule**

This previously surfaced as an opaque xcodebuild package-resolution failure:
“Couldn't update repository submodules.”

The bundle script now:

1. Tests Xcode Git's submodule support.
2. Discovers the active shell Git's exec path.
3. Verifies that directory actually contains git-submodule.
4. Exports the verified helper path for xcodebuild.
5. Re-tests support and fails with an actionable error if repair is impossible.

The distribution preflight and troubleshooting docs now record this behavior.
A replay with GIT_EXEC_PATH explicitly removed passed the strict bundle.

### 8.3 Canonical release and feature truth

README/spec/ADR surfaces now say:

- v0.7.2 is the current public stable release.
- The post-v0.7.2 reliability train is a 0.7.3 candidate.
- Meeting auto-stop, VAD chunking, cleaned-mic finalization, Cohere support, and
  live dictation preview shipped in the v0.7 line with their actual flags and
  defaults.
- The recent merged implementation plans are no longer described as open.

## 9. Candidate artifact evidence

The strict audit build command was:

**env -u GIT_EXEC_PATH REQUIRE_MEETING_ECHO_ASSETS=1
LOCALVQE_CMAKE_BUILD_JOBS=1 VERSION=0.7.3
scripts/dist/build_app_bundle.sh**

Verified bundled asset hashes:

| Asset | SHA-256 |
|---|---|
| AEC model | b6e43138588a83bfe903ab5e143b4020b91c1e1629f5a575ac5855ff0003c731 |
| liblocalvqe | 20355fc08cea119fad38abc17659795412054587ae8fcfd79cbfac5d37cb3e85 |
| ffmpeg | eaf91238e104dd0e262bc6510e25061855cc99a6955a721b0ac99660d58c473d |
| yt-dlp | 498bd0dae17855c599d371d68ec5bafc439a9d8640e838be25c765a9792f261b |
| node | d36b3d980963d44bd2c5e844fac4cfeee26a167b744287a4e74a9575af9d0559 |

These hashes prove the contents of the audit bundle, not the future signed DMG.
The final release must re-verify the immutable signed artifact.

## 10. Test and compiler evidence

### Hosted exact-main gate

Run 29532380793 passed:

- README reference validation
- Informational swift-format lint
- Release build
- CLI contract smoke
- Release bundle smoke
- Concurrency-warning build
- First-party Swift 6 build with WhisperKit skipped
- Full parallel Swift test suite

The log enumerated **4,919 of 4,919** XCTest-style tests and separately reported
**17** Swift Testing tests passed.

### Focused local gate

The focused high-risk matrix ran **1,220 tests with zero failures**. It covered
microphone routing/prewarm, capture lifecycle, meeting service, recovery and
finalization, STT routes/scheduler/leases, auto-save, CLI config/search,
Settings, and text processing.

The full suite was not repeated locally. The repository instructions limit it
to once per task, and exact-main hosted CI had already run it successfully.

### Current-toolchain Swift 6.3 finding

A separate local strict-language build was run with the current release
machine toolchain:

**MACPARAKEET_SKIP_WHISPERKIT=1 swift build
--build-path .build-swift6-audit -Xswiftc -swift-version -Xswiftc 6**

It failed at three calls in SettingsViewModel around lines 1217, 1254, and
1261. A main-actor-isolated captured value named service is sent into async
calls to CommandLineToolInstalling.currentStatus/install. The protocol does not
declare Sendable even though the production CommandLineToolInstallService is
an actor.

This is a **P1 concurrency/toolchain debt item**, not a blocker for the current
0.7.3 release configuration:

- The strict Xcode Release bundle passes in the package's Swift 5.9 language
  mode.
- Exact-main hosted CI's pinned Xcode 16.1 Swift 6 step is green.
- The failure appears under the newer Swift 6.3.1 checker on the release
  machine.

The repair should make the isolation/Sendable contract explicit and update test
doubles, then add a current-toolchain gate so hosted and release-machine truth
cannot drift.

The same build reports two warnings where non-Sendable completion handlers are
passed to ScreenCaptureKit closures annotated Sendable in SystemAudioStream
around lines 526 and 530.

### Warning baseline

The exact-main hosted log contains **9,785 unique swift-format findings** after
deduplicating the full path/line/message diagnostic:

- **6,269** indentation
- **2,011** add-lines
- **825** line-length
- **363** spacing
- **228** trailing-comma
- **67** remove-line
- **22** trailing-whitespace

Separately, there are **136 unique non-format compiler warning diagnostics**,
predominantly Sendable/concurrency warnings in production and tests. Counts
span multiple CI steps and are debt baselines, not distinct runtime bugs. The
current “Swift Concurrency Safety” step does not fail on warnings. Future work
should establish baseline-aware or changed-files ratchets rather than attempt
an unrelated tree-wide cleanup inside 0.7.3.

## 11. Static safety and repository hygiene

Recent additions contain:

- zero forced try operations
- zero forced casts
- zero fatalError/preconditionFailure calls
- one precondition that engineCount is greater than zero, representing an
  internal invariant

Across Sources, static regex review found:

- one forced try
- zero forced casts
- eleven fatalError/preconditionFailure sites

The whole-tree crash sites were inspected. They are primarily unavailable
coder initializers, impossible registry/schema encoding conditions, and an
explicit debug-only path; none was newly introduced by the reviewed release
range.

Repository security settings currently report:

- Dependabot security updates disabled
- Secret scanning disabled
- Non-provider secret patterns disabled
- Push protection disabled
- Validity checks disabled
- Vulnerability alerts API unavailable because the feature is disabled

These are repository-policy hardening opportunities. They do not substitute
for the performed source/log inspection, and enabling them should be a
deliberate maintainer action.

The preferred no-mistakes gate is not installed in this worktree environment,
so the documented local and hosted gates were used instead.

## 12. Architecture improvement portfolio

### A. Speech-model lifecycle module — strong, highest priority

**Evidence**

- STTRuntime is about 2,661 lines.
- It mixes inference ownership with static download, availability, deletion,
  cache layout, and telemetry behavior.
- EngineSettingsViewModel, SettingsViewModel, OnboardingViewModel,
  ModelsCommand, STTClient, and TranscriptionViewModel reach through different
  lifecycle implementations.
- FluidAudio 0.15.5's ModelHub change has a wide caller radius.

**Direction**

Create one lifecycle module that owns the model catalog, availability,
download, deletion, disk layout, operation telemetry, and quiescence needed for
disk mutation. Keep inference, leases, scheduling, warm-up, and active sessions
in STTRuntime/STTScheduler.

**Payoff**

- One policy for GUI and CLI.
- Smaller upstream-upgrade radius.
- Executable cache-mutation concurrency.
- Focused lifecycle tests.
- Less responsibility in STTRuntime without undoing ADR-016.

### B. Authoritative microphone intent — strong, hardware gated

System Default and a named microphone are different intents. Preserve System
Default as macOS's implicit route. When a user names an input, either open that
input or return a typed visible failure; do not silently record from a room mic.
Expose the effective route during capture and make preparation, Input Test,
start, recovery, and diagnostics share the same policy.

### C. Read-only Speech Memory query module — strong, strategic

Current search meanings differ:

- Library uses TranscriptionRepository.fetchLibraryPage.
- Dictation history uses DictationRepository.search.
- CLI search uses SegmentRepository.search and cited FTS segment hits.
- CLI history exposes additional transcription/dictation keyword paths.

Build one read-only module over the existing repositories and derived segment
index. It should own query interpretation, local filtering, ranking, context
budgeting, and citation provenance. GUI, CLI, and future agent integrations
should adapt to that shared meaning. It should not own LLM providers, chat, or
mutation.

Reconcile the tentative Context Engine and Meetings Workspace plans before
implementation.

### D. Current-toolchain concurrency boundary — strong engineering hardening

Make service protocols that cross actors explicit about Sendable and isolation.
Start with CommandLineToolInstalling and the Settings caller exposed by Swift
6.3.1. Audit ScreenCaptureKit completion closure annotations next. Add a
release-machine/current-Xcode CI lane or scheduled gate while keeping the
pinned reproducible release lane.

### E. Transcript-detail composed panes — worth exploring

TranscriptResultView is about 3,879 lines with more than forty local state
values spanning media, transcript editing/search, speakers, summaries, prompt
results, chat, retranscription, export, copy, and deletion.

Keep one composition screen but deepen coherent reader, timeline/speaker,
generated-results, chat, and media/export panes. Extend existing view-models
where they already own behavior. A file split that merely moves the same shared
state is not an architecture improvement.

### F. Settings domain composition — worth exploring

- SettingsView: about 3,921 lines
- SettingsViewModel: about 1,855 lines
- SettingsViewModelTests: about 3,124 lines
- SettingsRootViewModel: 97 lines

Make the root compose capture, engines/models, system/permissions,
storage/privacy, and integrations. Move persistence and behavior together one
domain at a time with compatibility forwarding. Do not create one view-model
per settings row.

## 13. Deliberate non-candidates

The following modules pass the deletion test and should not be reorganized just
because they are large:

- **DatabaseManager:** the chronological migration ledger is easier to audit in
  one place.
- **STTRuntime/STTScheduler control plane:** central inference ownership is the
  right depth. Extract lifecycle policy, not per-engine runtimes.
- **Meeting settlement/finalization:** another service would duplicate the
  current recovery and ownership seam.
- **Text-processing pipeline:** it already centralizes ordered shared behavior.
- **AppEnvironment:** it is a composition root; blindly splitting it would
  distribute construction side effects.
- **Repository interfaces:** they are useful user-data/test seams. Add Speech
  Memory query semantics above them.
- **Capability registry:** Phase A already exists and is complete. Do not
  rediscover it as missing architecture.

## 14. Smaller hardening opportunities

These are bounded follow-ups, not reasons to expand the release patch:

1. Bound the podcast RSS episode array to prevent unbounded feed memory growth.
2. Turn the meeting live chunker's single-consumer serialization comment into
   an executable invariant.
3. Revisit scheduler quiesce/cache-clear exclusion before cache clearing is
   exposed as an in-process GUI operation.
4. Ratchet formatting and concurrency warnings from a checked baseline.
5. Run local-link/ADR/contract checks for docs-only changes; current CI paths
   can skip documentation changes.
6. Stop routinely committing raw diagnostic logs.
7. Enable appropriate GitHub dependency and secret-scanning protections.
8. Archive old VERIFY-THEN-ARCHIVE plan rows after reconciling their actual
   implementation state.
9. Migrate FluidAudio to ModelHub through the lifecycle module.
10. Evaluate WhisperKit 1.x independently; remove the Swift 6 skip only after
    proving compatibility.

## 15. Required signed-candidate physical QA

Run these checks from a notarized app copied to **/Applications**, not from the
DMG volume.

### Microphone and route matrix

1. System Default input with built-in output.
2. System Default input while AirPods/Bluetooth output is active.
3. Explicit built-in microphone with Bluetooth output.
4. Explicit AirPods microphone.
5. Explicit USB microphone.
6. A multichannel USB interface, including a non-first active channel.
7. Disconnect and reconnect routes before capture and during a session.
8. Confirm the visible effective route matches the buffers actually received.

### Instant Dictation

1. Leave the app idle for at least five minutes.
2. Run five immediate start/stop cycles.
3. Turn Instant Dictation off and on.
4. Change routes between and during cycles.
5. Sleep/wake the Mac and repeat.
6. Confirm disabling Instant Dictation leaves no idle orange microphone
   privacy indicator.

### Meeting lifecycle

1. Run longer than the #808 reporter's failure window.
2. Stop normally.
3. Stop during startup/preparation.
4. Exercise app quit/relaunch recovery.
5. Confirm final transcript, recovery artifacts, auto-save feedback, and no
   stuck recording state.

### No-headphones echo cancellation

Run representative Zoom, Google Meet, and Teams calls. Compare:

- raw microphone
- system audio
- playback/reference
- cleaned microphone
- final transcript

Evaluate real speech overlap and speaker bleed, not only synthetic tones.

### Installed CLI

From the installed app:

1. Run the installer/status/permissions flow.
2. Run the release demo smoke.
3. Exercise help, version, health, config, and cited search.
4. Confirm GUI and CLI use the same intended database and permissions.

## 16. Release publication sequence

Only after section 15 passes:

1. Select the exact approved Git SHA and record it.
2. Confirm origin/main still contains reviewed PR #822 at or after 6599603c.
3. Build once from the approved SHA with strict asset requirements.
4. Developer-ID sign all nested code and the application.
5. Create the DMG, notarize it, and staple it.
6. Copy/install into /Applications and run the complete installed-app smoke.
7. Hash the final immutable DMG and record its byte length.
8. Generate the Sparkle signature from that exact file.
9. Upload that one DMG.
10. Update and deploy the appcast with the matching version, build, URL, length,
    and signature.
11. Attach the same DMG to GitHub release v0.7.3 for Homebrew consumers.
12. Verify the remote DMG length/hash, appcast signature/version, GitHub asset,
    and update discovery from a prior stable install.

Do not rebuild between signing, Sparkle signing, GitHub upload, and appcast
publication. Those surfaces must all describe the same bytes.

## 17. Audit branch inventory

### Release stabilization

- Package.swift — exact FluidAudio 0.15.4 pin.
- scripts/dist/build_app_bundle.sh — Xcode git-submodule helper detection and
  repair.
- docs/distribution.md — matching preflight and troubleshooting guidance.

### Canonical truth synchronization

- README.md
- Sources/MacParakeetCore/AppFeatures.swift comments
- spec/README.md
- spec/adr/023-activity-based-meeting-auto-stop.md
- plans/README.md
- plans/active/live-dictation-preview-readout.md

### Review artifacts

- This comprehensive audit.
- 2026-07-16-release-readiness-audit.md — concise release-focused companion.
- 2026-07-16-architecture-deep-dive.md — architecture-focused companion.

## 18. Reproduction commands for a future reviewer

Use a clean worktree based on freshly fetched origin/main.

- Compare release range:
  **git diff --stat v0.7.2..origin/main**
- Verify exact commits:
  **git rev-parse origin/main** and **git rev-parse v0.7.2^{commit}**
- Focused tests:
  **swift test --filter AreaTests**
- Full gate, once:
  **swift test**
- Release bundle:
  **REQUIRE_MEETING_ECHO_ASSETS=1 VERSION=0.7.3
  scripts/dist/build_app_bundle.sh**
- Package resolution:
  **swift package show-dependencies**
- README references:
  **scripts/check-readme-references.sh**
- Hygiene:
  **git diff --check**
- Current Swift 6 language check:
  **MACPARAKEET_SKIP_WHISPERKIT=1 swift build -Xswiftc -swift-version
  -Xswiftc 6**

Before trusting an old result, refresh origin, GitHub releases, the appcast,
exact-head CI, open PRs, and issue comments. This document is exact for the
baseline and date at its top; live state can move afterward.

## 19. Final handoff

The correct near-term posture is:

- Keep the release number at **0.7.3**.
- Preserve the narrow dependency/build-script/docs fixes.
- Do not pull architecture refactors into the release.
- Treat the Swift 6.3 Sendable finding as the first post-release engineering
  hardening item unless it can be fixed and independently verified without
  destabilizing the candidate.
- Treat merged PR #822 as part of the candidate and repeat its Settings visual
  checks in the signed app.
- Complete the signed physical matrix.
- Publish one immutable artifact through every distribution surface.

The strongest first post-release architecture slice is a speech-model lifecycle
module small enough to move FluidAudio from DownloadUtils to ModelHub without
touching every caller. The strongest correctness slice is authoritative
microphone intent with physical route verification. The strongest strategic
slice is a shared local, read-only Speech Memory query and citation contract.

Broad rewrites, a 0.8 version jump, or splitting large files for appearance
would add risk without solving the verified problems.
